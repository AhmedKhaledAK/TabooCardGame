# Copy to .env . Vite inlines VITE_* vars into the client bundle at build time,
# so only public values belong here. The Discord client ID is public by design.
VITE_DISCORD_CLIENT_ID=your_discord_application_id
